# CEN 4072 Group Project

To build with GCC make sure you have a build folder. Then from the root directory for the repository run:

`make all`

After building you can run the program by running the following command:

`./build/program`

# How to Use

Coming soon...